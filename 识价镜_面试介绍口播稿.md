# 识价镜 面试介绍口播稿

> 配合《识价镜_Agent_技术面试演示.pptx》14 页使用。括号内为对应页码。
> 完整版约 4 分钟；文末附 2 分钟精简版裁法。

---

## 开场（第 1–2 页，约 40 秒）

各位好，我介绍的项目是识价镜——一个 AI 拍照识物与智能比价购物 Agent。

它面向的场景是：用户看到一件商品，却不知道品牌、型号，也构造不出准确的搜索词；直接拿图做向量搜索，只能找到“长得像”的结果，根本不敢用来比价。所以我做的不是一个识图模型，而是一条可恢复的 Agent 工作流，把“识别 → 检索 → 决策”这条购物链路完整打通。接下来我分四部分讲：工作流怎么编排、检索怎么控制约束、怎么防止误比价、稳定性怎么保障。

## 第一部分：Workflow Agent 架构（第 3–6 页，约 60 秒）

整个系统用 LangGraph StateGraph 编排，链路是七个节点：输入校验、识别修正、意图约束、混合召回、同款 SKU、排序证据、响应。有两个设计想重点讲。

第一，所有节点共享一份 AgentState，通过条件边路由，一共四个决策点：入口有图片走 VLM 识别、没图片走修正写回；约束缺失或冲突进澄清、补齐后再继续；检索零结果只放宽低置信约束重查一次；检索层失败直接 FAILED——每条异常都有明确去向，不会堵死主链路。这份状态里按职责分了六组字段——识别结果、购物约束、检索查询、比价组，以及恢复用的版本号和脏标记；节点只写自己负责的字段，脏标记决定哪些下游要重算。第二，多轮修正只做局部重算：用户说“品牌不是 A，是 B”，修正会先校验识别编号、把修正写回状态、给下游节点打脏标记，然后只重算受影响的那一段——不再重复调用 VLM，上游已确认的结果全部保留。会话用 checkpoint 落盘，中断后从最近成功状态恢复，request_id 保证幂等。

## 第二部分：RAG 商品检索（第 7–9 页，约 50 秒）

检索上，我把商品标题、品牌、类目和关键属性向量化，构建 Milvus 索引。查询侧把 VLM 识别结果和用户筛选意图重写成 RetrievalQuery，字段拆成：查询文本、硬过滤、软词、负词。

召回是四路融合：文本向量、关键词、图像相似度、元数据过滤。四路各取 Top 100，按商品并集去重、截断到 200；因为量纲不同，先把每通道分数在候选集内 min-max 归一化，再加权融合成 recall_score 降序输出——有图像时 dense 0.35、sparse 0.20、image 0.25、metadata 0.20，纯文本时 dense 0.50、sparse 0.30、metadata 0.20，缺通道按可用通道重新归一化。每个候选保留每通道分数和来源，平台、价格、店铺类型等元数据随候选保留，供后续筛选和解释使用。这里有个权限边界是我刻意设计的：查询改写模型只能改写查询文本和软词，预算、平台、用户明确过的品牌型号这些硬过滤，模型没有权限修改。Milvus 不可用时自动降级本地词法检索，响应里明确标记 fallback，不伪装成向量检索。

## 第三部分：同款匹配与多阶段排序（第 10–12 页，约 40 秒）

比价最怕“长得像但不是同款”。我的做法分三步。

先做字段标准化：统一品牌、型号、规格，把属性分成身份、变体、描述三类；然后同款判定先做硬冲突否决，品类、品牌、型号有冲突直接排除，没有冲突才融合标题语义、属性重合度、图像相似度和来源键打分，用 complete-link 聚类成 SPU；最后按变体属性拆 SKU，只有同一 SKU 才进入同一个比价组，组内聚合最低价、平台均价和店铺标签。排序和价格计算——包括实付价等于价格减券加运费——全部是确定性代码，不依赖 LLM。

## 第四部分：AI Pipeline 稳定性与评测（第 13–14 页，约 40 秒）

模型输出不可靠，所以每个模型节点都有 Pydantic 结构校验加领域语义检查；失败时把错误反馈给修复循环，最多修两次，再失败就走规则或模板降级，并且在响应里如实标注。

可观测性上，按节点记录输入输出哈希、耗时、重试次数、降级状态和错误码——不记录密钥和模型内部思维链。

## 收尾（第 14 页说完即止，约 15 秒）

总结一句话：让模型负责理解，让系统负责正确、恢复和验证。这个项目的价值不在于调了几个模型，而在于把模型的不确定性关进了明确的工程边界里。我的介绍到这里，谢谢。


---

## 2 分钟精简版裁法

保留：开场 → 第一部分（只讲局部重算一点）→ 第二部分（只讲硬过滤权限）→ 第三部分（只讲三步中的“同一 SKU 才比价”）→ 收尾。

## 口径备注（面试前必读）

1. **技术栈口径**：简历技术栈行写的是 Python 3.11 / FastAPI / SSE / LangChain / Doubao-Seed-2.0-lite。代码实际是 **Python 3.12 / LangGraph / Pydantic / 火山方舟 Ark（OpenAI 兼容协议，模型名走环境变量）/ Milvus / SQLite & Postgres**，仓库没有 Web API 层。口播稿按真实口径写。若面试官对着简历问 FastAPI/SSE 实现，答不上来比写错更伤——建议在面试前把简历那行改掉，或至少背熟口径：“仓库只实现 Agent 核心，API/SSE 层在规划中”。
2. **“跨平台”口径**：平台以契约 ID（taobao/jd/pinduoduo）标识，仓库内是样例数据，没有真实抓取。被问就说“协议层按多平台设计，数据是种子样例”。
3. **评测数字口径**（如被追问）：248 测试、4/4 阻断指标都是真实的，但样本量小（n=2~8），回答时主动带上“种子回归”的限定词。
