# ── Secrets & environment ──
.env
*.env
.env.local
.env.*.local
*.env.local
secrets/
*.pem
*.key
*.jks

# ── OS files ──
Thumbs.db
.DS_Store
Desktop.ini

# ── IDE & editors ──
.idea/
.vscode/
*.iml
*.swp
*.swo
*~

# ── Logs ──
*.log
*.err.log

# ── Archives ──
*.tar.gz
*.zip

# ── Runtime artifacts ──
.codex-run/
artifacts/
backup-before-restart-*/
__pycache__/
*.pyc

# ── Backend (Java / Maven) ──
backend/target/
backend/*.log

# ── Flutter / Dart ──
app/build/
app/.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages

# ── Uploads (user-generated, not for repo) ──
uploads/*
!uploads/.gitkeep
!uploads/.gitignore
