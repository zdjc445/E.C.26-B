name: app_recommendation
description: Evidence-based purchase recommendations — buy/wait/avoid decision support.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.4.0
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  app_core:
    path: ../core
  app_search:
    path: ../search
  flutter_riverpod: ^2.5.0
  dio: ^5.4.0
  go_router: ^14.0.0
