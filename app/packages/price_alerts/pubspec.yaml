name: app_price_alerts
description: Price alerts module — create, list, update, and delete price alerts.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.4.0
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  app_core:
    path: ../core
  flutter_riverpod: ^2.5.0
  dio: ^5.4.0
  go_router: ^14.0.0
