name: app_core
description: Core infrastructure for the shopping assistant app — Failure, Either, Network, Cache, Theme, TokenStore.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.0
  flutter: ">=3.32.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.0
  dio: ^5.4.0
  flutter_secure_storage: ^9.2.0
  connectivity_plus: ^6.0.0

dev_dependencies:
  mocktail: ^1.0.0
