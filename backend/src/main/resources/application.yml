server:
  port: 8080

spring:
  application:
    name: shopping-agent
  servlet:
    multipart:
      max-file-size: 8MB
      max-request-size: 8MB

app:
  upload-dir: ${UPLOAD_DIR:../uploads}

logging:
  level:
    root: INFO
    org.springframework: INFO
